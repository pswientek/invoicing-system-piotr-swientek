server:
  port: 7777

logging:
  level:
    org:
      springframework:
        boot: INFO
    pl:
      futurecollars:
        invoicing: INFO
        invoicing.db: DEBUG

spring:
  profiles:
    active: file
  flyway:
    enabled: false
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  security:
    user:
      name: piotr
      password: abc123

invoicing-system:
  allowedOrigins: http://localhost:4200
  csrf:
    disable: true
jwt.secret: piotr